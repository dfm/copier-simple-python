#include <nanobind/nanobind.h>

namespace nb = nanobind;
using namespace nb::literals;

double add_two_numbers(double a, double b) { return a + b; }

NB_MODULE(lib{{ python_project_name }}, m) {
  m.doc() = "A simple example of an extension module built with nanobind";
  m.def("add_two_numbers", &add_two_numbers, "a"_a, "b"_a,
        "Add two floating point numbers together");
}
